{
  "name": "coteditor-win",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight Windows port of CotEditor (Wails + Monaco + tree-sitter)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dompurify": "^3.4.12",
    "markdown-it": "^14.3.0",
    "monaco-editor": "^0.52.0",
    "tree-sitter-wasms": "^0.1.13",
    "web-tree-sitter": "^0.25.0"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "@types/markdown-it": "^14.1.2",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
